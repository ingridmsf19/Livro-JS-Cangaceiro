class Negociacoes {

    constructor() {
        this._negociacoes = [];
    }

    adiciona(negociacao) {
        this._negociacoes.push(negociacoes);
    }

    paraArray() {
        return [].concat(this._negociacoes);
    }

}